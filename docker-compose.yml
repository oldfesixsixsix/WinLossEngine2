services:
  winloss-tracker:
    build: .
    container_name: winloss_tracker
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./win-loss-data:/app/data
    environment:
      - NODE_ENV=production
